tailwind.config = {
    theme: {
        extend: {
            animation: {
                'blink': 'blink 1s steps(1) infinite',
                'typing': 'typing 4s steps(38) 1s 1 normal both, blink 1s steps(1) infinite'
            }
        }
    }
}
